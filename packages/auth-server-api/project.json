{
  "name": "auth-server-api",
  "tags": ["type:package"],
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "cwd": "packages/auth-server-api",
        "script": "build"
      },
      "dependsOn": ["^build"]
    },
    "dev": {
      "executor": "nx:run-script",
      "options": {
        "cwd": "packages/auth-server-api",
        "script": "dev"
      },
      "dependsOn": ["^build"]
    }
  }
}
